# dsh-zen-spoof 本地联调配置
# 通过 dsh plugin add 安装后不需要此文件
# 仅用于本地联调：dsh web --patch <此文件路径>
- insert:
    - id: zen-spoof
      name: file:///path/to/dsh-zen-spoof/dist/index.js
      config:
        providers:
          - opencode
